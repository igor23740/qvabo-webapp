# Локальная папка Claude Code: скиллы, настройки сессии.
# gsap-skills внутри — отдельный git repo, требует submodule-setup для публикации.
.claude/

# Секреты — НИКОГДА не коммитить (неукоснительное правило владельца).
.env
.env.*
*.env
*.key
*.pem
*.pfx
secrets.json
credentials.json
